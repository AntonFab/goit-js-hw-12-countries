{{#each this}}
<h2>{{name}}</h2>
<div>
    <p>Capital: {{capital}}</p>
    <p>Population: {{population}}</p>
    <ul><b>Languages</b>
        {{#each languages}}
        <li>{{name}}</li>
        {{/each}}
    </ul>
    <img src="{{flag}}" alt="Flag of {{name}}" width="300">
</div>
{{/each}}